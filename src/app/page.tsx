// import { BlackoutTwoAM } from './layout';
import { Octokit } from '@octokit/rest';

const Page = async () => {
  // const text = await getMarkdown()

  return (
    <main className="min-h-screen bg-primary overflow-clip w-screen justify-center flex flex-col items-center ">
      <div className="h-screen flex items-center justify-center">
        <h1 className="font-humane text-secondary  text-[80vh] scale-y-150 pt-[12%]">
          TEFKAH
        </h1>
      </div>
    </main>
  );
};

export default Page;
